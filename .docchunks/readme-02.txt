
End-to-end vertical slice: LLM discovery to CapabilityArtifact to deterministic replay against a hostile legacy bank mock UI, with origin/path allowlists, irreversible-action policy, redaction, and HITL fail-closed handoff.
